/**
 * pivot mode data-set test when value in row.
 */
 describe('Table Mode Test When Value In Row', () => {
 
  describe('test 1', () => {
    test('should get 1', () => {
      expect(1).toBe(1);
    });
 })
});
